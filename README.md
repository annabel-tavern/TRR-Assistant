# TRR-Assistant
Helps auto-apply filters when shopping on the real real (ie, make one filter that is "size: med, sort $$$ to $, condition: good) &amp; also save things with different labels (instead of just favoriting things). 
